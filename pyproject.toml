[project]
name = "langraph_assistant"
version = "0.1.0"
description = "A Python project for Langgraph notes assistant."
authors = [
    { name="Your Name", email="your.email@example.com" }
]
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn"
]

[tool.setuptools]
packages = ["langraph_assistant"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"